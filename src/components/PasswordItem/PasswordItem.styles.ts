import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    borderColor: '#dddddd',
    borderWidth: 1,
    padding: 15,
    borderRadius: 4,
    marginBottom: 10
  }
});

export default styles;
